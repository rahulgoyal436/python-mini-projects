# Test generated by RoostGPT for test roost-test using AI Type Open AI and AI Model gpt-4

# Import necessary modules
import unittest
from add import add_numbers

class TestAddNumbers(unittest.TestCase):
    def test_Add_numbers_4beee899f4(self):
        # Test case 1: Check with positive integers
        result = add_numbers(10, 5)
        self.assertEqual(result, 15, "Should be 15")

        # Test case 2: Check with negative integers
        result = add_numbers(-3, -2)
        self.assertEqual(result, -5, "Should be -5")

        # Test case 3: Check with positive and negative integers
        result = add_numbers(10, -5)
        self.assertEqual(result, 5, "Should be 5")

        # Test case 4: Check with zero
        result = add_numbers(0, 5)
        self.assertEqual(result, 5, "Should be 5")

        # Test case 5: Check with float numbers
        result = add_numbers(1.5, 2.5)
        self.assertEqual(result, 4.0, "Should be 4.0")

if __name__ == '__main__':
    unittest.main()
